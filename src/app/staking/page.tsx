"use client";

import DashboardPage from '@/app/dashboard/page';

// Mirror the original Dashboard staking UI on the /staking route.
export default function StakingPage() {
  return <DashboardPage />;
}
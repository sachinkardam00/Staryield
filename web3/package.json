{
  "name": "staryield-web3",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "export-abi": "node scripts/export-abi.js",
    "build-abi": "npm run compile && npm run export-abi",
    "deploy:testnet": "hardhat run scripts/deploy-testnet.js --network bscTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.5",
    "ethers": "^6.13.1",
    "hardhat": "^2.22.5",
    "dotenv": "^16.4.5"
  }
}
